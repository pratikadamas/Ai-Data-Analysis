# Task Progress Tracker - Authentication & Profile Integration

- [ ] Install dependencies and update backend settings (`pyjwt`, `config.py`)
- [ ] Create MongoDB database connection module (`mongodb.py`)
- [ ] Create authentication and JWT utility functions (`auth.py`)
- [ ] Create SMTP mail sender with console fallback (`mail.py`)
- [ ] Create rate-limiting mechanism (`rate_limit.py`)
- [ ] Implement authentication routes (`routes/auth.py`)
- [ ] Register new routes in the main application (`main.py`)
- [ ] Create frontend User Context (`UserContext.jsx`) and register API calls (`api.js`)
- [ ] Design glassmorphic Auth pages (Login, Register, OTP validation, Reset Password) (`Auth.jsx`)
- [ ] Design premium User Profile view (`UserProfile.jsx`)
- [ ] Integrate user session and profile into layout components (`App.jsx`, `Dashboard.jsx`, `Header.jsx`, `Sidebar.jsx`)
- [ ] Verify functionality (rate limiting, OTP verification, password resets, profile updates)
